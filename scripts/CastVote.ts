const main = async () => {};

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});

// Get a provider

// Get your signer from .env (should be voter)

// Receive address of ballot and proposal index

// Interact with it
